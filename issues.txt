-----------------------
по JSON

answers это массив объектов, а не объект вида {title, value}

date некорректный формат, надо iso8601 использовать

также не хватает в каждом вопросе answer с правильным ответом / ответами из answers->value

programming, OC это теги внутри каждого вопроса, это не должно быть ключом, т.е для каждого вопроса есть массив tags

"input" : "checkbox",
лучше questionType: single / multiple / text, вместо привязки к конкретному html виджету

-----------------------
